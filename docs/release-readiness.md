# Public release blockers

The following inputs are still required before release:

- Confirm whether “MIT, Apache-2.0” means dual licensing under either license or
  select one license, then replace the non-license placeholder.
- Create and push the public repository.
- Pin an immutable public commit in the backend and pass cross-repository
  compatibility validation.

No placeholder metadata may remain in a release candidate.
